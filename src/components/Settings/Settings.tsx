import React from "react";
import s from './Settings.module.css'
const Settings = (props) => {
    return(
        <div className={s.Y}>
            Settings
        </div>
    )
}
export default Settings;